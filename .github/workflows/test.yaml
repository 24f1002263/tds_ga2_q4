name: IITM Test Action

on:
  push:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f1002263@ds.study.iitm.ac.in
        run: echo "Hello, world!"